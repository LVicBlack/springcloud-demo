#encrypt:
#  key: foo  # 设置对称密钥
encrypt:
  keyStore:
    location: classpath:/server.jks # jks文件的路径
    password: sspass                # storepass
    alias: maykey                   # alias
    secret: kkpass                  # keypass